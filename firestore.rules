rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /participants/{participantId} {
      allow read: if true;   // 🔥 allow public read
      allow write: if false; // block write from frontend
    }

  }
}